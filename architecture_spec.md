# 《羽毛球自动回合剪辑集锦生成》技术架构说明书 (MVP)

> **文档目标受众**：AI与开发人员
> **部署目标**：MVP阶段采用本地进程直连，后续演进为Docker容器化
> **核心交互**：视频切分 -> 过滤无效片段 -> 合并精彩集锦 -> 支持带逐帧步进的时间轴预览

## 1. 项目概述与业务流转

### 1.1 MVP 核心定义
本项目旨在开发一款《羽毛球自动回合剪辑集锦生成》网页。在 MVP（最小可行性产品）阶段，系统定位于**单机本地运行**的提效工具，不涉及多租户管理、复杂的用户鉴权及权限体系。核心目标是打通前端交互与底层基于 `TrackNetV3` 的羽毛球 AI 分析链路。

### 1.2 核心业务流转
1. **视频上传**：用户在前端上传羽毛球比赛原始视频。
2. **异步处理**：前端将请求发给 Python 后端，后端触发异步任务进行AI推理（回合拆分 & 过滤无效片段）。
3. **状态轮询**：前端通过轮询（Polling）接口定期获取后台任务处理的实时进度（进度条、当前处理阶段提示）。
4. **结果返回与预览**：后台处理完毕后，返回带时间宽度的 JSON 数据（参考 [rallies_test.json](file:///d:/Project/AIProject/badminton/TrackNetV3/rallies_test.json)）。前端渲染高级自定义视频播放器及时间轴结构。
5. **用户微调（可选）**：用户在前端时间轴上查阅（支持键盘快捷键逐帧调整和空格键启停），删减不想要的片段。
6. **集锦合成**：确认无误后，前端下发剪辑指令，后端根据最终时间轴合成精彩集锦。

### 1.3 输入输出与异常处理策略
- **输入支持**：1080P 分辨率，30fps 或 60fps 帧率的 MOV 或 MP4 格式视频。
- **输出产物**：经过提纯和合并的精彩集锦视频文件（MP4 或 MOV）。
- **容错机制（MVP版）**：为保证首次闭环的简单性，若后台处理程序崩溃或失败，接口端直接返回明确且友好的错误日志，通知用户重新上传处理，暂不引入复杂的断点续传或重试队列机制。
- **文件清理策略**：系统中需包含定时清理或基于会话的存储清理逻辑，以防止高帧率、大体积的视频文件耗尽本地服务器硬盘空间。

## 2. 系统架构与技术选型

为了实现前后端分离及职责单一，MVP 架构采用 **前后端解耦的本地双服务模式**：

### 2.1 整体架构拓扑
```text
[浏览器前端] <--(HTTP/REST)--> [Next.js BFF层] <--(HTTP/本地回环)--> [Python FastAPI AI层]
```

### 2.2 前端展现层 (Frontend)
- **底层框架**：采用 `Next.js` (React) 构建，提供完善的路由支持和组件化开发体验。
- **UI 组件库**：基于 `Tailwind CSS` 进行原子化样式控制，配合 `shadcn/ui` 搭建现代化、专业感强的控制台界面。
- **核心组件**：自定义视频播放器组件，需劫持原生行为，绑定键盘事件（左右键逐帧控制、空格播放暂停），并结合 HTML5 `<canvas>` 或自定义进度条绘制多段高亮的回合分布（即时间轴指示器）。

### 2.3 业务桥接层 (Next.js BFF API)
- **职责**：作为前端的代理（Backend for Frontend）。
- **主要工作**：接收浏览器的视频文件流上传，将大文件暂存到宿主机的本地目录中（如 `uploads/`）。落盘成功后，提取绝对路径，作为参数透传调用 Python 层接口。这分摊了 Python 层的网络并发压力。

### 2.4 AI 推理与视频处理层 (Python FastAPI)
- **底层框架**：使用 `FastAPI` 构建轻量级服务，对原有 `TrackNetV3` 脚本（如 `rally_detector.py`, `cut_rallies.py`）进行模块化导入和封装。
- **异步任务队列**：通过 Python 内置的 `concurrent.futures.ProcessPoolExecutor` 或等效的多线程隔离，为视频处理提供简易的后台排队与执行机制。收到任务后立即返回 `task_id`。
- **格式兼容转码（代理缓冲机制）**：为解决前端加载大体积原始 60fps MOV 产生的拖拽卡顿及格式不兼容现象，在 AI 推理完成前（或并行），FastAPI 内部会调用本地 `FFmpeg` 将原始视频转流为 720P/1080P 30fps 的标准 MP4（H.264+AAC）文件。该轻量级“代理视频”专门提供给前端用于时间轴预览和拖拽，而最终合并时仍提取原始的高清文件。

## 3. 核心接口契约与数据流

前后端的数据交互采用 RESTful API 进行，主要业务流程拆解为以下 4 个核心通信节点：

### 3.1 视频本地落盘 (Next.js BFF 端)
- **Endpoint**: `POST /api/upload`
- **行为**: 接收前端 `multipart/form-data` 的视频上传。考虑到大文件特性，BFF 层直接将流写入宿主机的 `uploads/` 目录。
- **返回**: 成功写入的本地绝对路径 `{"file_path": "/absolute/path/to/video.mov"}`。

### 3.2 触发异步 AI 分析 (Python AI 端)
- **Endpoint**: `POST /analyze`
- **行为**: Python 服务端主进程接收视频绝对路径，将处理推入后台 `ProcessPoolExecutor`，避免阻塞 HTTP 响应。
- **返回**: 立即返回任务凭证 `{"status": "processing", "task_id": "abc-123"}`。

### 3.3 轮询查询与结果获取 (Python AI 端)
- **Endpoint**: `GET /task/{task_id}/status`
- **行为**: 前端基于轮询机制（如每 2 秒一次）请求此接口。
- **返回**:
  - *处理中*: `{"status": "processing", "progress": 45, "stage": "extracting_frames"}`
  - *已完成*: `{"status": "completed", "rallies": [{"start": 12.5, "end": 18.2}, ...], "proxy_video_url": "/static/proxy/abc-123.mp4"}`

### 3.4 确认微调并生成集锦 (Python AI 端)
- **Endpoint**: `POST /clip_highlight`
- **行为**: 接收前端用户在时间轴上微调、筛选后的最终回合时间段数组。此接口调起本地 FFmpeg，基于**原始高清单个视频文件**进行无损（`copy` 指令）或高质量重新编码截取，并执行物理拼接。
- **返回**: 最终合成集锦的下载链接 `{"status": "success", "download_url": "/static/outputs/highlight_abc-123.mp4"}`。

## 4. 视频处理策略与前端交互实现

为了提供极佳且无卡顿的专业剪辑体验，必须在前端播放组件和后端转码上进行特定策略设计。

### 4.1 代理缓冲池策略 (Proxy Video Strategy)
由于原始视频（如 iPhone 拍摄的 60fps MOV 格式）存在极大码率且部分浏览器原生兼容性极差，直接利用 HTML5 `<video>` 播放并支持高频拖拽极易导致浏览器卡死内存溢出。
- **策略实现**: 在 Python 端生成任务的同时，并行执行一个粗糙且快速的 FFmpeg 转流进程（例如转换成 720P, 30fps 的 H.264 MP4编码格式），作为 **Proxy Video**。
- **交互逻辑**: 前端界面加载并播放的是这个 Proxy Video 用于时间轴预览。只有在最后合成导出（第 3.4 环节）时，Python 端才会拿着时间戳去找 **Original Video** 进行切片拼接，从而兼顾流畅播放与高清产出。

### 4.2 高级时间轴交互逻辑
前端需要抛弃浏览器原生控制条（Controls），完全重写自定义视频组件：
- **键盘事件劫持**: 
  - `Space (空格)`: 切换播放/暂停状态。
  - `ArrowLeft / ArrowRight`: 利用预设的帧率（例如基于 Proxy Video 30fps，每按一次 `currentTime` 增减 `0.033` 秒），实现精准“逐帧”步进。
- **时间轴数据可视化**: 根据 `rallies.json` 中定义的回合段落（`start` 和 `end`），在进度条容器之上覆盖渲染醒目的高亮区块（Segment Blocks）。用户点击区块可快速跳转（Seek）并可提供删除特定回合（剔除无效片段）的操作面板。
