# 羽毛球 AI 回合识别与剪辑踩坑记录

本文记录项目在模型调优和自动剪辑中的已验证经验，避免后续重复试错。

## 稳定处理链路

```text
原始 MOV
-> 720p30 代理视频
-> TrackNet 球轨迹
-> 运动分类器
-> 音频死球空档切分
-> rallies_ai.json
-> 1080p60 HDR 成片
```

当前 TrackNet 稳定参数：

```text
batch_size=8
eval_mode=nonoverlap
max_sample_num=100
large_video=true
```

这些参数与项目正式 AI 服务和训练数据的生成方式一致。

## 1. 背景采样过大会耗尽内存

### 现象

`max_sample_num=1800` 在 720p 视频上生成背景中位图时，尝试申请约 4.9GB 连续内存并触发 `MemoryError`。

### 处理

长视频固定使用：

```text
--max_sample_num 100
```

固定机位只需要少量均匀采样帧即可估计背景，没有必要加载上千帧。

## 2. batch size 不是越大越快

### 现象

RTX 4060 Laptop 8GB 使用 `batch_size=16` 时显存接近满载，推理速度反而从约 2.6 秒/批下降到约 12 秒/批。

### 处理

本机使用：

```text
--batch_size 8
```

判断批次是否合适时要同时观察速度和显存，不能只看是否发生 OOM。

## 3. 推理模式必须与训练数据一致

### 现象

`weight` 完整滑窗模式计算量约为 `nonoverlap` 的数倍，而且与当前分类模型的训练输入分布不一致。

### 处理

使用项目正式配置：

```text
--eval_mode nonoverlap
```

更复杂的 TrackNet 推理方式不一定能提高最终回合识别准确率。切换模式前必须重新生成训练数据并回归全部标注视频。

## 4. 识别耗时要分阶段记录

至少分别记录：

- 代理视频生成时间；
- TrackNet 和回合识别时间；
- 剪辑编码时间；
- 包含失败重试的实际墙钟时间。

不要把失败测试时间混入成功流程的性能指标，也不能只报告理想耗时而隐藏用户实际等待时间。

## 5. 识别与剪辑不能重复判断边界

当 AI 已输出明确的 `start/end` 时，剪辑器必须将其作为 explicit intervals 直接执行。

不要在编码阶段再次运行运动检测来估算终点，否则第二套规则可能破坏已经调好的回合边界。

## 6. 长运动片段需要音频死球切分

球员捡球、走动或把球挑给对方时，画面仍有明显运动，容易把两个回合合并。

当前处理规则：

- 仅检查时长不小于 15 秒的候选片段；
- 寻找最大的、不小于 4 秒的击球声空档；
- 只有切分后两侧均不小于 5 秒才执行；
- 每个原始候选最多切分一次，避免连续误切。

运动信号适合生成候选，击球音频更适合判断死球间隔。

## 7. 技术验证不能代替真实验收

自动剪辑至少检查：

- 输出为 1920x1080、约 60fps；
- HEVC Main 10、HLG/HDR 标签正确；
- 原始音频保留；
- 开头、中间、结尾均能独立解码；
- 三个验证时间点确实不同，并目视确认画面有效。

曾出现验证日志将中点和结尾都打印成同一个时间点的情况，因此不能只相信 `verify passed` 文本。

## 8. 无人工标注视频只能作为 AI 粗剪

解码成功只能证明文件可播放，不能证明没有：

- 误检；
- 漏检；
- 起点过早；
- 终点过晚；
- 多回合合并。

新视频应先生成 AI 草稿，再通过 HighlightCutter 删除误检、补充漏检和修正边界。校正结果继续进入全量评估，不能只优化最新样本。

## 回归验收标准

当前只评估人工标注中时长不小于 5 秒的回合，匹配标准为时间区间 `IoU >= 0.5`。

```text
总体 Precision >= 0.75
总体 Recall >= 0.80
总体 F1 >= 0.75
多回合合并错误较基线减少至少 50%
任一已标注视频 F1 回退不超过 0.05
```

每次修改模型、候选分段或推理参数后，都必须重新运行全部已标注视频的评估。

## 本次实测参考

测试视频：`2026_01_07_19_22_33_IMG_0159.MOV`

```text
原片时长：443.76 秒
AI 回合：20 个
成片时长：199.44 秒
代理生成：98 秒
成功推理：603.03 秒
有效识别总耗时：701.03 秒
剪辑编码：45.86 秒
```

正式成片通过独立开头、中间、结尾解码检查，并保留 1080p60、10-bit HLG HDR 和双声道音频。
